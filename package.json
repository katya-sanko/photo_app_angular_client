{
  "name": "angular-client",
  "private": true,
  "version": "0.0.0",
  "description": "Photoapp project with AngularJS",
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}